# My Understanding

---

## 1. แบ่งหน้า Component

แยกโค้ดออกเป็นหลายๆ ไฟล์ (Components) จะได้ไม่อัดแน่นในหน้าเดียว อ่านง่าย แก้ง่าย และอาจจะเพิ่มอะไรอีกได้ง่าย:

- **App.jsx**: ตัวควบคุมหลัก ทำหน้าที่สลับหน้าไปมา (Routing) ระหว่างหน้า Home กับ Owner มี Navbar ติดอยู่ด้านบนตลอด
- **Navbar.jsx**: แถบเมนูให้กดเปลี่ยนหน้า (Home / Owner)
- **Owner.jsx**: หน้าแสดงรูปโปรไฟล์และประวัติส่วนตัว
- **Home.jsx**: "ศูนย์รวมสมอง" เก็บข้อมูลหลักทั้งหมด คอยยิง API ดึงข้อมูล คอยสั่งลบ และส่งข้อมูลไปให้ไฟล์อื่นใช้งาน
- **AdminForm.jsx**: ฟอร์มกรอก Name, Last Name, Position แล้วกด Save เพิ่มข้อมูล
- **UserTable.jsx**: ตารางแสดงรายการสมาชิก ปรับตัวตามโหมดได้ ถ้าเป็นโหมด Admin จะมีปุ่ม Delete เพิ่มขึ้นมา

---

## 2. ตัวแปรเก็บข้อมูล State Variables

ใช้ State ในหน้า Home.jsx ทั้งหมด 5 ตัว เป็นศูนย์กลางข้อมูล:

- **sector**: เช็คว่าตอนนี้เปิดหน้าไหนอยู่ ("" = หน้าแรกสุด, "user" = หน้า User, "admin" = หน้า Admin)
- **members**: เก็บ "รายการคนทั้งหมด" ที่ดึงมาจาก API เอาไปวาดเป็นตาราง
- **name**: เก็บตัวอักษรที่พิมพ์ในช่อง Name
- **lastName**: เก็บตัวอักษรที่พิมพ์ในช่อง Last Name
- **position**: เก็บตัวอักษรที่พิมพ์ในช่อง Position

---

## 3. วิธีการส่งและจัดการข้อมูล State Management

- สร้าง State ไว้ที่ไฟล์แม่ Home.jsx แล้วส่งข้อมูลผ่าน Props ลงไปให้ไฟล์ลูก AdminForm.jsx และ UserTable.jsx
- ใช้วิธีนี้เพราะ เว็บนี้ไม่ได้ใหญ่หรือซับซ้อน การเก็บ State ไว้ที่หน้า Home แล้วส่งต่อให้ไฟล์ลูกตรงๆ เข้าใจง่ายที่สุด เช็คเส้นทางข้อมูลง่าย ไม่ต้องลงเครื่องมือเพิ่มให้วุ่นวาย

---

## 4. ทำไมต้องใช้ useEffect (และการใส่ [])

ในหน้า Home.jsx ใช้ useEffect สั่งดึงข้อมูลจาก API ทันทีที่เปิดหน้าเว็บ:

- อยากให้เว็บดึงข้อมูลสมาชิกจาก API มาเตรียมไว้ทันทีที่หน้าเว็บโหลดเสร็จ คนดูจะได้เห็นตารางข้อมูลเลยโดยไม่ต้องกดปุ่มอะไรก่อน
- ใส่ [] (Array เปล่า) เพื่อบอก React ว่า "ให้ดึงข้อมูลแค่ครั้งเดียวตอนเปิดหน้าเว็บพอแล้ว" ถ้าไม่ใส่ [] ตัว React จะคอยดึงข้อมูลใหม่ทุกครั้งที่หน้าจอเปลี่ยน
  ทำให้ยิง API ไม่หยุดจนเว็บค้าง (Infinite Loop)

---

## 5. ลองใช้ fetch() โดยไม่ผ่าน useEffect

- ใช้ตอนที่มี User Action เช่น การกดปุ่มบันทึกหรือกดปุ่มลบ
- ตอนกด Save (เพื่อเพิ่มข้อมูล) และกด Delete (เพื่อลบข้อมูล) จะยิง fetch() ในฟังก์ชัน handleSave กับ handleDelete ได้เลยทันที เพราะต้องการให้ส่งข้อมูลไปที่ API เฉพาะตอนกดปุ่มเท่านั้น ไม่ได้อยากให้รันอัตโนมัติเหมือนตอนเปิดหน้าเว็บ

---

## 6. ใช้ fetch() แบบ Asynchronous (async/await)

รูปแบบที่ใช้: Asynchronous (async/await)

- ใช้อันนี้เพราะ การดึงข้อมูลผ่านเน็ตต้องใช้เวลา ถ้าใช้แบบ Synchronous หน้าเว็บจะค้าง กดอะไรไม่ได้เลยเพื่อรอข้อมูล แต่พอใช้ Asynchronous เบราว์เซอร์จะไปแอบดึงข้อมูลอยู่หลังบ้าน ทำให้หน้าเว็บยังลื่นไหล กดปุ่มต่างๆ ได้ตามปกติระหว่างรอข้อมูล

---

## 7. เพิ่มเติมเกี่ยวกับ React ที่ได้ลองใช้ในการเขียนครั้งนี้

- Controlled Components: ช่องกรอกข้อมูลทั้งหมดใน AdminForm ถูกควบคุมด้วย State ของ React (value={name} และ onChange) ทำให้ข้อมูลในฟอร์มกับตัวแปรตรงกันตลอดเวลา
- การเปิด-ปิดการแสดงผล (Conditional Rendering):
  - ใช้คำสั่งเช็คเงื่อนไข ({sector === "admin" && ...}) เพื่อเลือกโชว์หรือซ่อนฟอร์มและปุ่มลบตามโหมดที่เลือก
  - คำนวณความกว้างตารางอัตโนมัติ (sector === "admin" ? "w-1/4" : "w-1/3") ให้ตารางจัดสัดส่วนสวยงาม ไม่ว่าจะมี 3 หรือ 4 คอลัมน์
- การอัปเดตข้อมูลให้สดใหม่ (Re-fetching): หลังจากกดเพิ่มหรือกดลบข้อมูลสำเร็จ จะสั่งให้เรียกฟังก์ชัน fetchMembers() ซ้ำอีกรอบ เพื่อไปดึงข้อมูลล่าสุดจาก API มาอัปเดตตารางบนหน้าจอทันที
